#tuple is an immutable datatype in python

a = () #this is an empty tuple
a = (1,) # tuple with only one elements needs comma


tuple = (1,2,3,4, "manish")
print(tuple)
print(tuple[0])

tuple[0] = 'change' #this will give error as tuple is immutable